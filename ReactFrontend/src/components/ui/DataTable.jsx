import { Edit2, Trash2 } from "lucide-react";
const format = (value) => value === null || value === undefined || value === "" ? "—" : typeof value === "boolean" ? (value ? "Active" : "Inactive") : String(value).replace("T", " · ").slice(0, 30);
export default function DataTable({ columns, rows, onEdit, onDelete, renderActions }) { if (!rows.length) return <div className="empty-state">No records yet. Create the first one to get started.</div>; return <div className="table-wrap"><table className="data-table"><thead><tr>{columns.map((column) => <th key={column}>{column.replace(/([A-Z])/g," $1")}</th>)}<th>Actions</th></tr></thead><tbody>{rows.map((row, index) => <tr key={row.id ?? index}>{columns.map((column) => <td key={column}>{column === "isActive" ? <span className={`badge ${row[column] ? "success" : "neutral"}`}>{format(row[column])}</span> : format(row[column])}</td>)}<td className="row-actions">{renderActions?.(row)}<button className="table-button" onClick={() => onEdit(row)} aria-label="Edit"><Edit2 size={15}/></button><button className="table-button danger" onClick={() => onDelete(row)} aria-label="Delete"><Trash2 size={15}/></button></td></tr>)}</tbody></table></div>; }
