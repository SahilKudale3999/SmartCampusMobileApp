import { useAuth } from "../../../app/AuthContext";
import { resources } from "../../resources/resourceDefinitions";
import { Link } from "react-router-dom";
export default function DashboardPage() { const { user } = useAuth(); const modules = resources.filter((r) => r.roles.includes(user?.role)); return <section className="page"><header className="page-head"><div><p className="eyebrow">Control center</p><h2>Good to see you, {user?.fullName?.split(" ")[0] || "there"}</h2><p className="page-subtitle">Manage your campus operations from one secure workspace.</p></div></header><div className="metric-grid"><article className="metric-card"><span>Role</span><b>{user?.role}</b><small>Access is tailored to your account</small></article><article className="metric-card"><span>Available modules</span><b>{modules.length}</b><small>Ready for action</small></article><article className="metric-card"><span>Account</span><b>Active</b><small>{user?.email}</small></article></div><h3 className="section-title section">Your workspace</h3><div className="dashboard-grid">{modules.map((m,index)=><Link className="nav-card" to={`/${m.path}`} key={m.key}><span className="card-index">{String(index+1).padStart(2,"0")}</span><b>{m.label}</b><span>Open and manage {m.label.toLowerCase()}</span></Link>)}</div></section>; }
